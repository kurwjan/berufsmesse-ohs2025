---
interface Props {
    class?: string;
}

const { class: className, ...rest } = Astro.props;
---

<div
    class=`flex bg-[--primary-container-color] p-3 gap-3 rounded-xl items-center ${className}`
    {...rest}
>
    <slot />
</div>
